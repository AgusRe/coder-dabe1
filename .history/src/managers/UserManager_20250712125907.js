const fs = require(fs).promises;

class UserManager {
    constructor(path) {
        this.path = path;
    }

    async addUser(user) {

    
    async getUsers() {
        try {
            const data = await fs
        }
    }
}