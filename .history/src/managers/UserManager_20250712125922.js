const fs = require(fs).promises;

class UserManager {
    constructor(path) {
        this.path = path;
    }
    
    async getUsers() {
        try {
            const data = await fs
        }
    }

    async addUser(user) {
        try {
            
        }
    }
}